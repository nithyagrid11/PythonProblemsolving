import re
class Solution(object):
    def countAndSay(self, n):
        """
        :type n: int
        :rtype: str
        """
        c = '1'
        for _ in range(n-1):
            c = re.sub(r'(.)\1*', lambda m: str(len(m.group(0))) + m.group(1), c)
        return c
